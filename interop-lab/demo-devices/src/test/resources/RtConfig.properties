#
# default configuration setting for spring application context
#
mdpnp.domain=0

# multicast address for peer discovery is optional; pass down null
# if not specified and the system will use the defaults.
#dds.discovery.peers=239.255.0.1
dds.discovery.peers=
